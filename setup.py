from setuptools import setup, find_packages
setup(
    name="dca",
    version="0.0.0",
    author='',
    author_email='',
    install_requires=['numpy', 'scikit-learn', 'torch', 'torchvision'],
    packages=find_packages()
)
